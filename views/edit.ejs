<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main min-h-screen text-white px-10 py-5 sm:p-10 w-full bg-[#121212]">
      <div class="flex items-center gap-3 justify-end">
        <a
          class="inline-block border-[1px] px-5 py-1 border-blue-600 rounded-md text-zinc-300 hover:bg-blue-500 transition-all duration-300 hover:text-white mb-10 sm:mb-5 font-semibold"
          href="/profile/upload"
          >Upload Photo</a
        >
        <a
          class="inline-block border-[1px] px-5 py-1 border-red-600 rounded-md text-zinc-300 hover:bg-red-500 transition-all duration-300 hover:text-white mb-10 sm:mb-5 font-semibold"
          href="/logout"
          >Logout</a
        >
      </div>
      <h1 class="text-2xl font-semibold mb-4 text-center">
        <span class="font-light">Hello</span>, <%= user.name %> 👋
      </h1>
      <h5 class="text-center">You can edit this post below</h5>
      <form
        class="w-full sm:w-[450px] items-center flex flex-col mx-auto my-10"
        action="/update/<%= post._id %>" method="post"
      >
        <textarea
          class="resize-none rounded-md outline-none bg-transparent border-[1px] px-3 py-2 border-zinc-800 w-full h-72 sm:h-32 mb-5"
          rows="5"
          placeholder="What's on your mind?"
          name="content"
          id=""
        ><%= post.content %></textarea>
        <input
          class="font-semibold rounded-md px-6 py-1 bg-yellow-500 hover:text-zinc-900 text-zinc-800 hover:bg-purple-500 transition-all duration-300 w-fit"
          type="submit"
          value="Save Edit"
        />
      </form>
    
    

     
    </div>
  </body>
</html>
